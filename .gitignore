# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor files
.vscode/
.idea/

# Node
node_modules/

# Licensed font originals — keep local, do not publish (public repo)
# Root-anchored: bare 'FONTS/' would also match public/fonts/ (macOS ignorecase)
/FONTS/
